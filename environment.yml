# Conda env for the GUI + test suite. Core tooling (ingest, checksums,
# validate, query) needs none of this; it runs on the Python stdlib alone.
name: seqledger
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - streamlit>=1.40
  - pandas>=2.0
  - plotly>=5.20
  - pytest>=7.0
